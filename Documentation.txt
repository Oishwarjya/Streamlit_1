This file is to track documentation of this smaller scale streamlit project
virtual envirionment for python ->venv

To run the app, run command : "streamlit run app.py"

Step 1:
Download the requirements.txt file as -> "pip install -r requirements.txt"
